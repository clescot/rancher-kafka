kafka:
  labels:
    io.rancher.scheduler.affinity:host_label: zk=rancher
    io.rancher.scheduler.affinity:container_label_ne: io.rancher.stack_service.name=$${stack_name}/$${service_name}

  image: clescot/rancher-kafka:7
  ports:
   - "9092/tcp"
#   - "7203:7203"
  metadata:
      example:
        name: hello
        value: world
      example2:
        foo: bar


